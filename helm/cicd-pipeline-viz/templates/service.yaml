apiVersion: v1
kind: Service
metadata:
  name: {{ include "cicd-pipeline-viz.fullname" . }}
  labels:
    {{- include "cicd-pipeline-viz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cicd-pipeline-viz.selectorLabels" . | nindent 4 }}
